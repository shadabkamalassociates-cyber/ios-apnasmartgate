# Copy to .env and set your backend base URL (no trailing slash).
# BACKEND_URL=https://society.kamalhousing.com/api
# For local: BACKEND_URL=http://YOUR_IP:5001/api
BACKEND_URL=

# Razorpay (public key only — keep RAZORPAY_KEY_SECRET on the server, not in the app)
RAZORPAY_KEY_ID=
